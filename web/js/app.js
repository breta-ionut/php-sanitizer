/**
 * In this file are defined all the actions that need to be done when the application starts.
 */
(function () {
    
    $(document).ready(function () {
        // When the page is ready, start the executor.
        PHPS.Executor.start();
    });

})();
